# The only env var the frontend needs
# In production on Netlify: leave blank (API calls go through /api/* proxy in netlify.toml)
# In local dev: point to your backend
VITE_API_URL=https://your-railway-app.up.railway.app 
